server:
  port: 80
spring:
  redis:
    host: 10.9.216.1
    port: 6379
    pool:
      max-idle: 100
      min-idle: 1
      max-active: 1000
      max-wait: -1
    timeout: 300
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/eshop-inventory?useUnicode=true&characterEncoding=UTF-8&useSSL=true
mybatis:
  mapper-locations: classpath:mybatis/*.xml